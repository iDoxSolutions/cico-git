@model Cico.Models.Employee
@using Cico.Models.Helpers;           
@using (Html.BeginForm())
{
    
        @Html.Partial("_employeeFields")
        <input type="submit" value="Save" name="Save"/>
     @Html.HiddenFor(model => model.Id)
     @Html.HiddenFor(model => model.UserId)
   
    
}