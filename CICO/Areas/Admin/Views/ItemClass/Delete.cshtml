@model Cico.Models.CheckListItemTemplate

@{
    ViewBag.Title = "Delete Checklist Item";
    
}

<h2>Delete Checklist Item</h2>


@using (Html.BeginForm())
{
    <div>
        @Model.Description
    </div>
    <div>
        @Html.HiddenFor(c => c.CheckListItemTemplateId)
        <button type="submit">Delete</button>
    </div>
}